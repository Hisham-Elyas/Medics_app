import 'package:flutter/material.dart';

class NoDataFond extends StatelessWidget {
  const NoDataFond({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('No Data Fond'.toUpperCase())),
    );
  }
}
